mixin slide-status(currentSlide, nav)

  .slide-status
    .slide-status__content
      .slide-status__title!= currentSlide.title
      .slide-status__progress <strong>#{nav.current}</strong> / #{nav.total}